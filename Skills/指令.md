ChatGPT说的对，三处修正直接给Cursor指令：
请修改以下文件：
第一处： G:\Cursor\kato-modbus\F411_FreeRTOS_CubeMX\BSP\Inc\bsp_ads1115.h
找到：
c/* V/LSB：±6.144V量程 */
#define ADS1115_LSB             (0.0001875f)
在它后面加一行：
c/* 关闭比较器（低两位=11） */
#define ADS1115_COMP_DISABLE    (0x03)
第二处： G:\Cursor\kato-modbus\F411_FreeRTOS_CubeMX\BSP\Src\bsp_ads1115.c
找到：
c        uint16_t cfg = ADS1115_OS_START
                     | MUX_TABLE[ch]
                     | ADS1115_PGA_6144
                     | ADS1115_MODE_SINGLE
                     | ADS1115_DR_128SPS;
改成：
c        uint16_t cfg = ADS1115_OS_START
                     | MUX_TABLE[ch]
                     | ADS1115_PGA_6144
                     | ADS1115_MODE_SINGLE
                     | ADS1115_DR_128SPS
                     | ADS1115_COMP_DISABLE;
第三处： G:\Cursor\kato-modbus\F411_FreeRTOS_CubeMX\algorithm\Src\sensor_task.c
找到：
c    BSP_ADS1115_Init();
    BSP_SHT30_Init();
改成：
c    if (BSP_ADS1115_Init() != HAL_OK) {
        /* ADS1115初始化失败，挂起任务等待硬件就绪 */
        vTaskSuspend(NULL);
    }
    if (BSP_SHT30_Init() != HAL_OK) {
        /* SHT30初始化失败，使用默认温湿度继续运行 */
        temperature = 25.0f;
        humidity    = 60.0f;
    }
第四处： G:\Cursor\kato-modbus\F411_FreeRTOS_CubeMX\Middlewares\Third_Party\FreeRTOS\Source\include\FreeRTOSConfig_template.h
找到：
c#define configTOTAL_HEAP_SIZE             ((size_t)(15 * 1024))
改成：
c#define configTOTAL_HEAP_SIZE             ((size_t)(24 * 1024))
不要添加configIDLE_TASK_STACK_SIZE。